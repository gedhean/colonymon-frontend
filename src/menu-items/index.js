// project import
import pages from './pages';
import config from './config';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [pages, config]
};

export default menuItems;
